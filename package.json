{
  "name": "poke-idle-vscode",
  "displayName": "PokeIdle VS Code",
  "description": "Un idle game Pokémon dans votre barre latérale VS Code.",
  "version": "0.1.0",
  "engines": {
    "vscode": "^1.75.0"
  },
  "categories": [
    "Other"
  ],
  "activationEvents": [],
  "main": "./out/extension.js",
  "contributes": {
    "viewsContainers": {
      "activitybar": [
        {
          "id": "poke-idle-explorer",
          "title": "PokeIdle",
          "icon": "media/pokeball-icon.png"
        }
      ]
    },
    "views": {
      "poke-idle-explorer": [
        {
          "type": "webview",
          "id": "poke-idle-view",
          "name": "Pokémon Safari"
        }
      ]
    }
  },
  "scripts": {
    "vscode:prepublish": "npm run compile",
    "compile": "tsc -p ./",
    "watch": "tsc -watch -p ./",
    "pretest": "npm run compile && npm run lint",
    "lint": "eslint src --ext ts",
    "test": "node ./out/test/runTest.js"
  },
  "devDependencies": {
    "@types/vscode": "^1.75.0",
    "@types/node": "16.x",
    "typescript": "^4.9.5"
  }
}
